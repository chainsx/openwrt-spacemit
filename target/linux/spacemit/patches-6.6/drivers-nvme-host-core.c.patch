diff -Nuar -r --no-dereference a/drivers/nvme/host/core.c b/drivers/nvme/host/core.c
--- a/drivers/nvme/host/core.c	2024-11-22 22:38:37.000000000 +0800
+++ b/drivers/nvme/host/core.c	2025-05-18 01:44:45.155665140 +0800
@@ -1912,6 +1912,9 @@
 	 * value of the Atomic Write Unit Power Fail parameter.
 	 */
 	blk_queue_physical_block_size(disk->queue, min(phys_bs, atomic_bs));
+#if defined(CONFIG_SOC_SPACEMIT_K1X)
+	blk_queue_max_segment_size(disk->queue, 0x80000);
+#endif
 	blk_queue_io_min(disk->queue, phys_bs);
 	blk_queue_io_opt(disk->queue, io_opt);
 
