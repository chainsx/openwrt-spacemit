diff -Nuar -r --no-dereference a/drivers/base/firmware_loader/main.c b/drivers/base/firmware_loader/main.c
--- a/drivers/base/firmware_loader/main.c	2024-11-22 22:38:37.000000000 +0800
+++ b/drivers/base/firmware_loader/main.c	2025-05-18 01:44:34.432651829 +0800
@@ -1241,7 +1241,7 @@
 	fw_work->device = device;
 	fw_work->context = context;
 	fw_work->cont = cont;
-	fw_work->opt_flags = FW_OPT_NOWAIT |
+	fw_work->opt_flags = FW_OPT_NOWAIT | FW_OPT_NO_WARN |
 		(uevent ? FW_OPT_UEVENT : FW_OPT_USERHELPER);
 
 	if (!uevent && fw_cache_is_setup(device, name)) {
